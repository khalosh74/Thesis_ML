# Scripts

Kept scripts support the release-era workflow.

- `generate_demo_dataset.py`: generates demo synthetic dataset assets
- `release_hygiene_check.py`: checks release bundle consistency
- `verify_official_artifacts.py`: validates run artifact contracts
